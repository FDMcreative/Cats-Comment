<!DOCTYPE html>
<html>

  <head>
    <meta charset="utf-8">
    <title>Cats Comment</title>

    <% include ./partials/stylesheets %>

  </head>

  <body>

    <header>
      <% include ./partials/navbar %>
    </header>

    <% for(const type in messages) { %>
      <div class="<%= type %>">
        <div class="container">
          <%= messages[type] %>
        </div>
      </div>
    <% } %>

    <main class="container">
      <%- body %>
    </main>

    <% include ./partials/script %>

  </body>

</html>
