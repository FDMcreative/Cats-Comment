<!DOCTYPE html>
<html>

  <head>
    <meta charset="utf-8">
    <title>The FaceboOok</title>
    <% include ./partials/stylesheets %>
  </head>

  <body>

    <header>
      <% include ./partials/navbar %>
    </header>

    <% for(const type in messages) { %>
      <div class="<%= type %>">
        <h5><%= messages[type] %></h5>
      </div>
    <% } %>

    <main>
      <%- body %>
    </main>

    <% include ./partials/script %>

  </body>

</html>
